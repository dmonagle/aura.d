module aura._feature_tests.graph;

version (featureTest) {
	import aura.feature_test;



	unittest {


		feature("Graph store and retrieve", (f) {
				f.scenario("Create a graph and store models in it", {
						
					});			
			});
	}
}